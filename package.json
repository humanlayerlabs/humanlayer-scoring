{
  "name": "@humanlayerlabs/scoring",
  "version": "1.0.0",
  "description": "Open source scoring algorithm for HumanLayer Protocol — compute humanity scores and tiers from on-chain and off-chain signals.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src tests --ext .ts"
  },
  "keywords": [
    "humanlayer",
    "soulbound",
    "identity",
    "reputation",
    "web3",
    "base",
    "scoring"
  ],
  "author": "HumanLayer Labs <info@humanlayer.network>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.ts"]
  }
}
